const Cart = require('../models/cart');


const handleGetCart = (req,res)=>{
    // Cart -> backend -> redux store ->ui
};

const handlePostCart = (req,res)=>{

};




module.exports = {
    handleGetCart,
    handlePostCart
}